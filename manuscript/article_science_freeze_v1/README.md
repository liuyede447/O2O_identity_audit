# Article science freeze v1

This is a provenance index, not a new outcome artifact. It consolidates pointers and SHA-256 values for the frozen primary sources, manuscript-generated tables, runner-free endpoint sensitivity, and dense common-trajectory sensitivity. No detector, replay, training, or scientific recomputation is performed by this ledger builder.

All manuscript numbers remain sourced from the listed immutable artifacts; the ledger does not replace or rewrite them.

See `CONSOLIDATED_MANIFEST.json` for the complete file-level list and hashes.
